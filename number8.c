//a program to find the ASCII value of input character
#include<stdio.h>
int main()
{
	char ch;
	printf("Enter the character that you want to find the ASCII value of:\n");
	scanf("%c",&ch);
	printf("The ASCII value is %d",ch);
	return 0;
}
